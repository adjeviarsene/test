#ifndef TEST_H
#define TEST_H

 struct triangle;
 struct triangle *new_triangel();
 void triangle_init(struct triangle*);
 void destroy_triangle(struct triangle*);
 int calcul(struct traingle*);
#endif